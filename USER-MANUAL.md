# 📘 คู่มือการใช้งาน Excel Report Code Generator

**เวอร์ชัน:** 2.0  
**วัตถุประสงค์:** แปลงไฟล์ Excel (.xlsx) เป็นโค้ดรายงานสำหรับ NPOI, JasperReports JRXML, และ RDLC

---

## 📑 สารบัญ

1. [ภาพรวม](#1-ภาพรวม)
2. [การติดตั้งและเปิดใช้งาน](#2-การติดตั้งและเปิดใช้งาน)
3. [การอัปโหลดไฟล์](#3-การอัปโหลดไฟล์)
4. [ระบบ Sections (ส่วนของรายงาน)](#4-ระบบ-sections-ส่วนของรายงาน)
5. [การ绑定ข้อมูล Fields และ Parameters](#5-การ绑定ข้อมูล-fields-และ-parameters)
6. [การใช้งาน RPPRINTIF / RP (สูตรเงื่อนไข)](#6-การใช้งาน-rpprintif--rp-สูตรเงื่อนไข)
7. [การส่งออก (Export)](#7-การส่งออก-export)
8. [การรองรับรูปภาพ](#8-การรองรับรูปภาพ)
9. [เคล็ดลับและข้อควรระวัง](#9-เคล็ดลับและข้อควรระวัง)
10. [แก้ไขปัญหาเบื้องต้น](#10-แก้ไขปัญหาเบื้องต้น)

---

## 1. ภาพรวม

**Excel Report Code Generator** เป็นเครื่องมือเว็บที่ช่วยแปลงไฟล์ Excel (.xlsx) ให้เป็นโค้ดรายงานในรูปแบบต่าง ๆ โดยอัตโนมัติ ช่วยประหยัดเวลาในการเขียนโค้ดรายงานและรักษาความถูกต้องของการจัดรูปแบบจากต้นฉบับ

### รูปแบบที่รองรับ

| รูปแบบ | ไฟล์ผลลัพธ์ | ใช้กับ |
|--------|-------------|--------|
| **NPOI** | `npoi-code.txt` | โค้ด C# สำหรับสร้าง Excel ด้วย NPOI library |
| **JRXML** | `jrxml-code.txt` | ไฟล์รายงานสำหรับ JasperReports |
| **RDLC** | `rdlc-code.txt` | ไฟล์รายงานสำหรับ SQL Server Reporting Services |

### คุณสมบัติเด่น

- ✅ ลากและวาง (Drag & Drop) หรือเลือกไฟล์จากเครื่อง
- ✅ รองรับไฟล์ที่มีหลายชีต (Multi-sheet)
- ✅ แสดงตัวอย่างข้อมูลแบบ Real-time พร้อมจัดรูปแบบเดิม
- ✅ ตรวจจับส่วนของรายงานอัตโนมัติจาก Column A
- ✅ ผูกข้อมูล Fields และ Parameters ด้วย syntax พิเศษ
- ✅ รองรับเงื่อนไข RPPRINTIF / RP
- ✅ รองรับการส่งออกรูปภาพ
- ✅ รองรับสี Theme จาก Excel

---

## 2. การติดตั้งและเปิดใช้งาน

### ข้อกำหนด

- เบราว์เซอร์สมัยใหม่ (Chrome, Firefox, Edge, Safari)
- ไม่ต้องติดตั้งโปรแกรมเพิ่มเติม เป็นเว็บแอปพลิเคชันล้วน

### วิธีเปิดใช้งาน

1. เปิดไฟล์ `index.html` ในเบราว์เซอร์
2. หรือรันผ่าน local server (เช่น `Live Server` ใน VS Code)

> ⚠️ **หมายเหตุ:** ต้องเปิดผ่าน web server ไม่สามารถเปิดไฟล์ HTML โดยตรงแบบ `file://` ได้ เนื่องจากต้องโหลด JavaScript modules จาก CDN

---

## 3. การอัปโหลดไฟล์

### วิธีที่ 1: ลากและวาง (Drag & Drop)

1. ลากไฟล์ `.xlsx` จากไฟล์เดอร์มาวางที่โซนอัปโหลด
2. ระบบจะโหลดและแสดงตัวอย่างข้อมูลโดยอัตโนมัติ

### วิธีที่ 2: เลือกไฟล์

1. คลิกที่ข้อความ **"เลือกไฟล์"** หรือคลิกที่โซนอัปโหลด
2. เลือกไฟล์ `.xlsx` จากเครื่องของคุณ

### ข้อมูลที่แสดงหลังโหลดไฟล์

| ข้อมูล | รายละเอียด |
|--------|-----------|
| **ชื่อไฟล์** | ชื่อไฟล์ที่อัปโหลด |
| **ขนาด** | ขนาดไฟล์ (KB/MB) |
| **จำนวนชีต** | จำนวนชีตทั้งหมดในไฟล์ |
| **เลือกชีต** | ดร็อปดาวน์สำหรับสลับชีต |

> 💡 **เคล็ดลับ:** สามารถเปลี่ยนชีตได้ตลอดเวลาโดยเลือกจากดร็อปดาวน์ "เลือกชีต"

---

## 4. ระบบ Sections (ส่วนของรายงาน)

ระบบจะตรวจจับส่วนของรายงานโดยอัตโนมัติจาก **Column A** ของแต่ละแถว

### วิธีกำหนด Sections

ใส่ชื่อส่วนใน **Column A** ของแถวแรกของแต่ละส่วน:

```
| Column A      | Column B        | Column C    | Column D     |
|---------------|-----------------|-------------|--------------|
| title         | รายงานสรุป         |             |              |
| columnheader  | ชื่อสินค้า         | จำนวน       | ราคา          |
| detail        | {{productName}}  | {{qty}}     | {{price}}    |
| pagefooter    | หน้า {PAGE}       |             |              |
```

### ชื่อ Sections ที่รองรับ

| ชื่อ Section | ชื่อที่ยอมรับได้ (Aliases) | คำอธิบาย |
|-------------|--------------------------|---------|
| **title** | — | ส่วนหัวเรื่องรายงาน |
| **pageheader** | `page header`, `page_header`, `page-header` | ส่วนหัวหน้า |
| **columnheader** | `header`, `colheader`, `col_header`, `column header`, `col-header` | หัวตาราง |
| **detail** | — | ส่วนข้อมูลหลัก (จะถูกวนลูป) |
| **columnfooter** | `colfooter`, `col_footer`, `column footer`, `col-footer` | ท้ายตาราง |
| **pagefooter** | `page footer`, `page_footer`, `page-footer`, `foot` | ส่วนท้ายหน้า |
| **summary** | — | ส่วนสรุป |
| **end** | — | จุดสิ้นสุด (ไม่ใช่ section จริง) |

### การแสดงผล Sections

หลังโหลดไฟล์ ระบบจะแสดง Sections ที่ตรวจพบเป็น **แท็กสี** แต่ละแท็กจะมี:

- **ชื่อ Section** (เช่น DETAIL, TITLE)
- **ช่วงแถว** (เช่น แถว 5 - 15)
- **แท็กความเข้ากันได้** (NPOI / JRXML / RDLC) แสดงว่าส่วนนี้รองรับในแต่ละรูปแบบหรือไม่

### ตารางความเข้ากันได้

| Section | NPOI | JRXML | RDLC |
|---------|------|-------|------|
| title | ✅ | ✅ | ✅ |
| pageheader | ✅ | ✅ | ✅ |
| columnheader | ✅ | ✅ | ✅ |
| **detail** | ✅ (Dynamic Loop) | ✅ (Dynamic Band) | ✅ (Tablix) |
| columnfooter | ✅ | ✅ | ❌ |
| pagefooter | ✅ | ✅ | ✅ |
| summary | ✅ | ✅ | ❌ |

> 📝 **หมายเหตุ:** Column A จะถูกลบออกก่อนส่งออก ไม่ต้องกังวลว่าชื่อ section จะติดไปในรายงาน

---

## 5. การ绑定ข้อมูล Fields และ Parameters

### 5.1 Field Binding (绑定ข้อมูลจาก DataTable)

ใช้ syntax `{{ชื่อฟิลด์}}` ในเซลล์เพื่อ绑定กับข้อมูลจาก DataTable:

```
| Column A      | Column B             | Column C           |
|---------------|---------------------|--------------------|
| title         | รายงาน               |                    |
| columnheader  | ชื่อสินค้า             | ราคา               |
| detail        | {{ProductName}}      | {{Price}}          |
```

**ตัวอย่างผลลัพธ์:**
- **NPOI:** `Convert.ToDouble(dtRow["ProductName"])`
- **JRXML:** `$F{ProductName}`
- **RDLC:** `Fields!ProductName.Value`

### 5.2 Parameter Binding (绑定ค่าพารามิเตอร์)

ใช้ syntax `P{{ชื่อพารามิเตอร์}}` เพื่อ绑定กับค่าที่กำหนดเอง:

```
| Column A      | Column B              |
|---------------|----------------------|
| title         | รายงานระหว่างวัน        |
| detail        | P{{reportDate}}       |
```

**ตัวอย่างผลลัพธ์:**
- **NPOI:** `Params["reportDate"]`
- **JRXML:** `$P{reportDate}`
- **RDLC:** `Parameters!reportDate.Value`

### 5.3 Mixed Expressions (นิพจน์ผสม)

สามารถผสมข้อความปกติกับ Fields/Parameters ได้:

```
{{ProductName}} - {{Category}}
รายงานวันที่ P{{reportDate}}
```

---

## 6. การใช้งาน RPPRINTIF / RP (สูตรเงื่อนไข)

ใช้ **Note/Comment** ของเซลล์ Excel เพื่อกำหนดเงื่อนไขการแสดงผล

### 6.1 RPPRINTIF (แสดง/ซ่อน เซลล์ตามเงื่อนไข)

ใส่สูตรใน **Note ของเซลล์**:

```
=RPPRINTIF({{Discount}} > 0)
```

**ความหมาย:** เซลล์นี้จะแสดงเฉพาะเมื่อมีส่วนลด (Discount > 0)

**ผลลัพธ์แต่ละรูปแบบ:**
- **NPOI:** `Func<DataRow, bool>` ตรวจสอบเงื่อนไข
- **JRXML:** `<printWhenExpression>`
- **RDLC:** `<Visibility><Hidden>=Not(...)</Hidden></Visibility>`

### 6.2 RP (แทนที่ค่าเซลล์ด้วยนิพจน์)

ใส่สูตรใน **Note ของเซลล์**:

```
=RP({{Price}} * {{Quantity}})
```

**ความหมาย:** แทนที่ค่าที่แสดงในเซลล์ด้วยผลคูณของ Price × Quantity

**ผลลัพธ์แต่ละรูปแบบ:**
- **NPOI:** `Func<DataRow, object>` คำนวณค่า
- **JRXML:** `<textFieldExpression>`
- **RDLC:** `=Fields!Price.Value * Fields!Quantity.Value`

### ตัวอย่างการใช้งาน Combined

```
| Column A    | Column B          | Note (ใน Excel)                    |
|-------------|-------------------|------------------------------------|
| title       | รายงานสินค้า        |                                    |
| columnheader| ชื่อ               |                                    |
| detail      | {{Name}}          |                                    |
| detail      | {{Total}}         | =RP({{Price}} * {{Qty}})           |
| detail      | ส่วนลด             | =RPPRINTIF({{Discount}} > 0)       |
```

---

## 7. การส่งออก (Export)

### ปุ่มส่งออก

หลังจากโหลดไฟล์แล้ว จะมีปุ่มส่งออก 3 ปุ่ม:

| ปุ่ม | สี | ผลลัพธ์ |
|------|-----|---------|
| **NPOI** | ⬛ ดำ | ไฟล์ `npoi-code.txt` — โค้ด C# |
| **JRXML** | 🟣 ม่วง | ไฟล์ `jrxml-code.txt` — ไฟล์ JasperReports XML |
| **RDLC** | 🔵 ฟ้า | ไฟล์ `rdlc-code.txt` — ไฟล์ RDLC XML |

### ขั้นตอนส่งออก

1. คลิกปุ่มรูปแบบที่ต้องการ
2. ไฟล์จะถูกดาวน์โหลดโดยอัตโนมัติ
3. นำโค้ดไปใช้งานตามรูปแบบที่เลือก

### รายละเอียดแต่ละรูปแบบ

#### 🔲 NPOI (C#)

โค้ดที่สร้างจะใช้ **NPOI library** สำหรับสร้างไฟล์ Excel ใน C#:

- สร้าง workbook, sheet, rows, cells
- จัดรูปแบบฟอนต์, สี, border, alignment
- รองรับ Merge cells
- Detail section จะถูกสร้างเป็น **DataRow loop**
- รองรับ Images (Base64)
- รองรับ Params dictionary

**วิธีใช้:**
```csharp
// 1. คัดลอกโค้ดจากไฟล์ npoi-code.txt
// 2. ใส่ข้อมูล vào DataTable (P_Data)
// 3. ตั้งค่า Params dictionary
// 4. รันโค้ดเพื่อสร้างไฟล์ Excel
```

#### 🟣 JRXML (JasperReports)

ไฟล์ XML สำหรับ JasperReports:

- รองรับ Portrait/Landscape (ตรวจจับอัตโนมัติ)
- แบ่ง Band ตาม Sections (title, columnHeader, detail, etc.)
- รองรับ Fields, Parameters, Variables
- รองรับ Images (Embedded Base64)
- Snap-to-grid สำหรับจัดตำแหน่ง

**วิธีใช้:**
```java
// 1. คัดลอกโค้ดไปใส่ไฟล์ .jrxml
// 2. ออกแบบ DataSource ให้ตรงกับ Fields
// 3. Compile และแสดงผลด้วย JasperReports
```

#### 🔵 RDLC (SQL Server Reporting Services)

ไฟล์ XML สำหรับ RDLC:

- รองรับ PageHeader, PageFooter
- Detail section ใช้ **Tablix** (ตารางแบบ Dynamic)
- รองรับ Fields, ReportParameters
- รองรับ EmbeddedImages
- แปลงหน่วยเป็น cm

**วิธีใช้:**
```csharp
// 1. คัดลอกโค้ดไปใส่ไฟล์ .rdlc
// 2. ออกแบบ DataSet ให้ตรงกับ Fields
// 3. 绑定 DataTable และ Parameters
// 4. แสดงผลใน ReportViewer
```

---

## 8. การรองรับรูปภาพ

- ระบบจะตรวจจับรูปภาพในไฟล์ Excel โดยอัตโนมัติ
- รูปภาพจะถูกฝัง (embed) ในโค้ดผลลัพธ์เป็น Base64
- ตำแหน่งรูปภาพจะถูกปรับตาม Sections ที่เกี่ยวข้อง
- รองรับทั้งรูปแบบ PNG, JPEG

> ⚠️ **ข้อจำกัด:** รูปภาพขนาดใหญ่อาจทำให้ไฟล์ผลลัพธ์มีขนาดใหญ่ขึ้น

---

## 9. เคล็ดลับและข้อควรระวัง

### ✅ สิ่งที่ควรทำ

1. **ใช้ Column A สำหรับ Sections เท่านั้น** — ไม่ควรใส่ข้อมูลอื่นใน Column A
2. **ตั้งชื่อ Sections ให้ถูกต้อง** — ใช้ชื่อที่ระบบรองรับ (ดูตารางใน Section 4)
3. **ทดสอบแต่ละรูปแบบก่อน** — ลองส่งออกทั้ง 3 รูปแบบเพื่อตรวจสอบผลลัพธ์
4. **ตั้งค่าความกว้างคอลัมน์ใน Excel** — ความกว้างจะถูกปรับตามในรายงานผลลัพธ์
5. **ใช้ Merge cells ใน Excel** — ระบบจะรักษาการ Merge ไว้ในโค้ดผลลัพธ์

### ❌ สิ่งที่ไม่ควรทำ

1. **ไม่ใส่ข้อมูลใน Column A นอกเหนือจาก Section names**
2. **ไม่ใช้ไฟล์ .xls (เก่า)** — ระบบรองรับเฉพาะ `.xlsx` เท่านั้น
3. **ไม่แก้ไขโค้ดผลลัพธ์โดยตรงโดยไม่เข้าใจ** — ควรเข้าใจโครงสร้างก่อนแก้ไข

### 💡 เทคนิคเพิ่มเติม

- **เปลี่ยนชีต** — ใช้ดร็อปดาวน์ "เลือกชีต" เพื่อดูข้อมูลจากชีตอื่น
- **กดปุ่ม "ยกเลิก"** — เพื่อโหลดไฟล์ใหม่
- **ดูแท็กสี** — เพื่อตรวจสอบว่าแต่ละ Section รองรับรูปแบบไหนบ้าง
- **ใช้ Aliases** — สามารถใช้ชื่อเช่น `header`, `colheader` แทน `columnheader` ได้

---

## 10. แก้ไขปัญหาเบื้องต้น

| ปัญหา | สาเหตุ | วิธีแก้ไข |
|--------|--------|-----------|
| ไม่สามารถโหลดไฟล์ได้ | ไฟล์ไม่ใช่ .xlsx | แปลงไฟล์เป็น .xlsx ก่อน |
| แสดง "ไม่มีส่วนกำหนดใน Column A" | ไม่มีข้อความใน Column A | ใส่ชื่อ Section ใน Column A ของแถวแรก |
| สีในรายงานเพี้ยน | ใช้สี Theme ที่ซับซ้อน | ตรวจสอบสีใน Excel ว่าเป็นสีมาตรฐานหรือไม่ |
| รูปภาพไม่ปรากฏ | รูปภาพมีขนาดใหญ่เกินไป | ลดขนาดรูปภาพใน Excel |
| ผลลัพธ์ JRXML มีปัญหา | ความกว้างคอลัมน์เกินหน้ากระดาษ | ลดความกว้างคอลัมน์ใน Excel หรือเปลี่ยนเป็น Landscape |

---

## ตัวอย่างไฟล์ Excel ต้นฉบับ

```
┌─────────────┬──────────────────┬──────────┬────────────┐
│ Column A    │ Column B         │ Column C │ Column D   │
├─────────────┼──────────────────┼──────────┼────────────┤
│ title       │ รายงานสรุปยอดขาย   │          │            │
├─────────────┼──────────────────┼──────────┼────────────┤
│ pageheader  │ บริษัท ABC จำกัด   │          │ วันที่ P{{date}}│
├─────────────┼──────────────────┼──────────┼────────────┤
│ columnheader│ ชื่อสินค้า         │ จำนวน    │ ราคา       │
├─────────────┼──────────────────┼──────────┼────────────┤
│ detail      │ {{ProductName}}   │ {{Qty}}  │ {{Price}}  │
├─────────────┼──────────────────┼──────────┼────────────┤
│ pagefooter  │ หน้า {PAGE}        │          │            │
└─────────────┴──────────────────┴──────────┴────────────┘
```

---

*คู่มือนี้สร้างขึ้นสำหรับ Excel Report Code Generator v2.0*
